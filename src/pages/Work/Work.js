import React from "react";
import { Navbar, Slider } from "../../components";
import { motion } from "framer-motion";
const Work = () => {
  return (
    <motion.div>
      <Navbar />
        <Slider />
    </motion.div>
  );
};

export default Work;
